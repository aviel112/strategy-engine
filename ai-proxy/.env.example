# מפתח מ-https://aistudio.google.com/apikey
GEMINI_API_KEY=AIza...

PORT=8080

# דומיינים שמותר להם לקרוא ל-proxy — מופרדים בפסיק
ALLOWED_ORIGINS=https://aviel112.github.io,http://localhost:7810

# אופציונלי — לכפות מודל מסוים. ריק = השרת בוחר את הזמין הטוב ביותר
GEMINI_MODEL=
